// // 2. Tapşırıq
// // Verilən ədədin neçə rəqəmdən ibarət olduğunu tapan funksiya yaz.

// function verilen(eded){
    let metn = String(eded){
    let count = "" 
    for(let i = 0 ; i <= metn.length - 1 ; i++){
        count ++ 
    }
    return count
}
console.log(verilen(1235))